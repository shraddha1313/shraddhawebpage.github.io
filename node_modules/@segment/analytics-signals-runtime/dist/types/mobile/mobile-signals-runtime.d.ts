import { SignalsRuntime } from '../shared/signals-runtime';
import { Signal } from './mobile-signals-types';
export declare class MobileSignalsRuntime extends SignalsRuntime<Signal> {
    private signalCounter;
    private maxBufferSize;
    constructor(signals?: Signal[]);
    add: (signal: Signal) => void;
    getNextIndex: () => number;
}
//# sourceMappingURL=mobile-signals-runtime.d.ts.map