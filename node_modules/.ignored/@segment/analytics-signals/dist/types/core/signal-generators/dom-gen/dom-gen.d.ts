import { SignalEmitter } from '../../emitter';
import { SignalGenerator } from '../types';
export declare class ClickSignalsGenerator implements SignalGenerator {
    id: string;
    register(emitter: SignalEmitter): () => void;
    private getClosestClickableElement;
}
export declare class FormSubmitGenerator implements SignalGenerator {
    id: string;
    register(emitter: SignalEmitter): () => void;
}
export declare const shouldIgnoreElement: (el: HTMLElement) => boolean;
export declare class OnNavigationEventGenerator implements SignalGenerator {
    id: string;
    register(emitter: SignalEmitter): () => void;
    private createCommonFields;
}
//# sourceMappingURL=dom-gen.d.ts.map