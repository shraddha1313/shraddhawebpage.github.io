import { SignalsIngestClient } from './signals-ingest-client';
export class SignalsIngestSubscriber {
    load(ctx) {
        this.ctx = ctx;
        this.client = new SignalsIngestClient(ctx.analyticsInstance.settings.writeKey, ctx.unstableGlobalSettings.ingestClient);
    }
    process(signal) {
        void this.client.send(signal);
    }
}
//# sourceMappingURL=index.js.map